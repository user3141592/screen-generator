
screen 1 /bin/zsh
screen 2 /bin/zsh

select 1
split -v
focus right
select 2
focus left

bindkey "^o" only
bindkey "^j" focus down
bindkey "^k" focus up
bindkey "^l" focus right
bindkey "^h" focus left
bindkey "^y" quit 
bindkey "^s" split
bindkey "^v" split -v
startup_message off
